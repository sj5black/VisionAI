torch
torchvision
pillow

fastapi
uvicorn[standard]
python-multipart
jinja2
requests
itsdangerous
bcrypt

# Optional (animal behavior/expression zero-shot)
open_clip_torch

# Video analysis (VisionAI webapp 영상 표정·기분 분석)
opencv-python

# OpenFace 2.0 호환 표정/행동 분석 (Action Units, head pose, gaze) - 영상 전용
pyfaceau

# 이미지 표정 분석 - 이미지 전용
deepface
tf-keras  # deepface 의존성

openai>=1.0.0
python-dotenv>=1.0.0
python-chess>=1.9  # 체스 규칙·UCI 엔진 연동 (Stockfish 대국 모드용)

# Stockfish 엔진 (체스 vs AI): pip 패키지 아님. 시스템에 별도 설치.
#   Ubuntu/Debian: sudo apt install stockfish
#   경로 지정: STOCKFISH_PATH 환경변수 (예: /usr/games/stockfish)